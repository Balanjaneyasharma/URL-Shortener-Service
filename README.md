# URL-Shortener-Service
A minimal ASP.NET Core Web API for shortening URLs using Clean Architecture (Domain-Driven Design). Built with EF Core and PostgreSQL (Neon), it provides endpoints to create, retrieve, redirect, and delete short URL mappings with built-in input validation and global exception handling.
